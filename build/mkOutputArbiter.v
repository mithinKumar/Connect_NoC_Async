//
// Generated by Bluespec Compiler, version 2025.07 (build 282e82e9)
//
// On Sun May  3 13:36:39 IST 2026
//
//
// Ports:
// Name                         I/O  size props
// select                         O     2
// CLK                            I     1 clock
// RST_N                          I     1 reset
// select_requests                I     2
// EN_next                        I     1
//
// Combinational paths from inputs to outputs:
//   select_requests -> select
//
//

`ifdef BSV_ASSIGNMENT_DELAY
`else
  `define BSV_ASSIGNMENT_DELAY
`endif

`ifdef BSV_POSITIVE_RESET
  `define BSV_RESET_VALUE 1'b1
  `define BSV_RESET_EDGE posedge
`else
  `define BSV_RESET_VALUE 1'b0
  `define BSV_RESET_EDGE negedge
`endif

module mkOutputArbiter(CLK,
		       RST_N,

		       select_requests,
		       select,

		       EN_next);
  input  CLK;
  input  RST_N;

  // value method select
  input  [1 : 0] select_requests;
  output [1 : 0] select;

  // action method next
  input  EN_next;

  // signals for module outputs
  wire [1 : 0] select;

  // register arb_token
  reg [1 : 0] arb_token;
  wire [1 : 0] arb_token$D_IN;
  wire arb_token$EN;

  // remaining internal signals
  wire [1 : 0] gen_grant_carry___d11,
	       gen_grant_carry___d13,
	       gen_grant_carry___d4,
	       gen_grant_carry___d8;
  wire arb_token_BIT_0___h845, arb_token_BIT_1___h911;

  // value method select
  assign select =
	     { gen_grant_carry___d8[1] || gen_grant_carry___d13[1],
	       !gen_grant_carry___d8[1] && !gen_grant_carry___d13[1] &&
	       (gen_grant_carry___d4[1] || gen_grant_carry___d11[1]) } ;

  // register arb_token
  assign arb_token$D_IN = { arb_token[0], arb_token[1] } ;
  assign arb_token$EN = EN_next ;

  // remaining internal signals
  module_gen_grant_carry instance_gen_grant_carry_3(.gen_grant_carry_c(1'd0),
						    .gen_grant_carry_r(select_requests[0]),
						    .gen_grant_carry_p(arb_token_BIT_0___h845),
						    .gen_grant_carry(gen_grant_carry___d4));
  module_gen_grant_carry instance_gen_grant_carry_1(.gen_grant_carry_c(gen_grant_carry___d4[0]),
						    .gen_grant_carry_r(select_requests[1]),
						    .gen_grant_carry_p(arb_token_BIT_1___h911),
						    .gen_grant_carry(gen_grant_carry___d8));
  module_gen_grant_carry instance_gen_grant_carry_0(.gen_grant_carry_c(gen_grant_carry___d8[0]),
						    .gen_grant_carry_r(select_requests[0]),
						    .gen_grant_carry_p(arb_token_BIT_0___h845),
						    .gen_grant_carry(gen_grant_carry___d11));
  module_gen_grant_carry instance_gen_grant_carry_2(.gen_grant_carry_c(gen_grant_carry___d11[0]),
						    .gen_grant_carry_r(select_requests[1]),
						    .gen_grant_carry_p(arb_token_BIT_1___h911),
						    .gen_grant_carry(gen_grant_carry___d13));
  assign arb_token_BIT_0___h845 = arb_token[0] ;
  assign arb_token_BIT_1___h911 = arb_token[1] ;

  // handling of inlined registers

  always@(posedge CLK)
  begin
    if (RST_N == `BSV_RESET_VALUE)
      begin
        arb_token <= `BSV_ASSIGNMENT_DELAY 2'd1;
      end
    else
      begin
        if (arb_token$EN) arb_token <= `BSV_ASSIGNMENT_DELAY arb_token$D_IN;
      end
  end

  // synopsys translate_off
  `ifdef BSV_NO_INITIAL_BLOCKS
  `else // not BSV_NO_INITIAL_BLOCKS
  initial
  begin
    arb_token = 2'h2;
  end
  `endif // BSV_NO_INITIAL_BLOCKS
  // synopsys translate_on
endmodule  // mkOutputArbiter

